Данный текст не претендует на принадлежность к русскому языку. Если какие-то замечания противоречат формулировке заданий на багтрекере, то прошу об этом сообщить.

Странные название переменных в TAnalysisTableModel::TestAddRemoveMoc. Например, const IDList actualPointSet = idFromModel.

Не заметил тестов на содержимое в ячейках в столбцах с анализами.

Для многих методов не стоит модификатора const, хотя по своему смыслу они никак не меняют состояние объекта. 

Есть мёртвые методы, которые нигде не используются. Даже в тестах. Например AnalysisTableModel::analysisCollection().

Не понимаю, зачем вызывать disconnect в ItemListView::~ItemListView().

Мало тестов на вывод идентификаторов в лексикографическом порядке.

Очень сложный код в ItemListModel::appendPointList. О производительности моделей сейчас думать ещё рано.

Для бенчмарков вывод в qDebug() лучше не использовать, а то в релизе ничего не будет видно.

Если изменения в коде касаются каких-то спецефических технических решений, как, например, добавление транзакций, то их надо описывать более подробно.

Не понимаю необходимости в вызове query.finish() и query.clear().

